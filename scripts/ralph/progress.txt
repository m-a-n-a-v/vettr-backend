# VETTR Backend AI Agent - Ralph Progress Log
Started: Tue Feb 24 23:13:27 EST 2026
Project: VETTR Backend - AI Agent Feature
---

## US-086: Create AI agent usage tracking database schema
Status: ✅ COMPLETE
Date: 2026-02-24
Details:
- Created src/db/schema/ai-agent-usage.ts with aiAgentUsage table
- Schema includes: id (uuid PK), userId (FK to users), questionId, ticker, askedAt (timestamp), date (for daily grouping)
- Added composite index on (userId, date) for fast daily count lookups
- Exported from src/db/schema/index.ts barrel file
- Build passes with zero errors
- **Learnings for future iterations:**
  - Pattern: pgTable with uuid PK using defaultRandom()
  - Pattern: Foreign keys use .references() with onDelete: 'cascade'
  - Pattern: Indexes defined in second parameter as callback function
  - Pattern: date type imported from drizzle-orm/pg-core for daily grouping
  - Pattern: All imports use .js extension (ESM requirement)
---

## US-087: Create AI agent question registry with all 18 questions
Status: ✅ COMPLETE
Date: 2026-02-24
Details:
- Created src/services/ai-agent-questions.ts with AiAgentQuestion interface
- Defined all 6 initial category questions: financial_health, analyst_view, insider_activity, valuation, earnings, red_flags
- Defined all 12 follow-up questions (2 per category): debt_analysis, cash_position, recent_actions, price_targets, top_holders, smart_money, peer_valuation, dividend_check, earnings_beats, earnings_outlook, critical_flags, flag_trend
- Exported helper functions: getInitialQuestions(), getFollowUpQuestions(parentId), getQuestionById(id), getAllQuestions()
- Each follow-up has parent_id linking to its initial question
- Build passes with zero errors
- **Learnings for future iterations:**
  - Pattern: Define constants arrays then export via functions for encapsulation
  - Pattern: Use {TICKER} placeholder in labels for dynamic replacement
  - Pattern: Filter/find operations for querying question collections
  - Gotcha: Escape single quotes in labels using backslash
---

## US-088: Implement financial health response generators
Status: ✅ COMPLETE
Date: 2026-02-24
Details:
- Created src/services/ai-agent-responders.ts with AiAgentResponseData interface
- Implemented respondFinancialHealth: Queries financial_summary + valuation_metrics, calculates Altman Z-Score (Safe >2.99 / Grey 1.81-2.99 / Distress <1.81), cash runway months, current ratio, debt-to-equity, FCF yield with status colors
- Implemented respondDebtAnalysis: Deep debt focus with debt-to-equity, debt-to-assets, interest coverage ratio, gross margin with contextual verdicts
- Implemented respondCashPosition: Cash runway months, operating cash flow, free cash flow, FCF yield with trend context
- Used parallel Promise.all queries for performance (pattern from fundamentals.service.ts)
- Used n() null-safe helper throughout to avoid NaN/Infinity issues
- Summaries are opinionated with **bold** markdown for key numbers, include company name, give clear assessment
- Build passes, test suite runs (6 pre-existing admin-analytics failures unrelated to this story)
- **Learnings for future iterations:**
  - Pattern: Add `if (!db) throw new InternalError('Database not available')` at start of each exported function
  - Pattern: Use getStockByTicker helper to validate ticker and get stock details
  - Pattern: Parallel queries with Promise.all for better performance
  - Pattern: n() helper function everywhere for null-safe numeric values
  - Pattern: formatNumber, formatLargeNumber helpers for consistent formatting
  - Pattern: Dynamic verdict logic based on calculated metrics
  - Gotcha: TypeScript requires db null check in every function that uses db, not just helpers
---

## US-089: Implement analyst consensus response generators
Status: ✅ COMPLETE
Date: 2026-02-24
Details:
- Implemented respondAnalystView: Queries analyst_consensus table, returns consensus rating (Buy/Hold/Sell), total analysts, buy/hold/sell split, price target with upside/downside percentage
- Implemented respondRecentActions: Queries analyst_actions table ordered by date descending, returns last 5 analyst actions with firm names, action types (upgrade/downgrade/initiate), grade changes (from → to), and dates
- Implemented respondPriceTargets: Queries analyst_consensus for price target mean/high/low, calculates upside percentages for all three values, provides verdict on valuation (undervalued/neutral/overvalued)
- Verdict logic: Strong Buy (>20% upside) = green, Buy (>0% upside) = green, Sell or <-10% = red, Hold = yellow
- Summaries use opinionated language: "Analysts are bullish on...", "positive momentum", "negative sentiment", "mixed signals"
- Added imports for analystConsensus and analystActions tables, plus desc for date ordering
- Build passes, tests pass (same 6 pre-existing admin-analytics failures)
- Files changed: src/services/ai-agent-responders.ts
- **Learnings for future iterations:**
  - Pattern: Use .orderBy(desc(table.dateColumn)) for recent-first sorting
  - Pattern: actions.map() to transform query results into details array
  - Pattern: Count different action types (upgrade/downgrade/initiate) with forEach and conditionals
  - Pattern: Date formatting with toLocaleDateString for consistency
  - Pattern: Handle null consensus data gracefully with early return + 'Unknown' verdict
  - Pattern: Calculate upside as ((target - current) / current) × 100
  - Gotcha: Format negative upside as downside in summary text for user clarity
---

## US-090: Implement insider activity response generators
Status: ✅ COMPLETE
Date: 2026-02-24
Details:
- Implemented respondInsiderActivity: Queries major_holders_breakdown + insider_transactions tables, returns ownership breakdown (insider/institutional/public %), institution count, buy/sell transaction counts, net share accumulation/reduction with opinionated verdicts (Bullish/Neutral/Bearish)
- Implemented respondTopHolders: Queries major_holders_breakdown + institutional_holders tables, returns institutional ownership %, insider ownership %, institution count, top 5 holder concentration, float held by institutions, lists top 3 holder names
- Implemented respondSmartMoney: Queries major_holders_breakdown + institutional_holders + insider_transactions tables, calculates institutional position change trend (increasing vs decreasing), insider buy/sell pattern, combines into smart money score (0-100) with signal (Accumulating/Neutral/Distributing)
- Verdict logic: Bullish (buyRatio >65% + net accumulation) = green, Bearish (buyRatio <35% + net selling) = red, Neutral = yellow
- Smart money scoring: 60% weight on institutional trend + 40% weight on insider pattern, score >65 = Accumulating (green), score <35 = Distributing (red)
- Summaries use directional language: "Smart money is flowing in", "Insiders are heading for the exits", "positive insider signals", "concerning insider selling"
- Added imports for sql, majorHoldersBreakdown, institutionalHolders, insiderTransactions
- Build passes, tests pass (same 6 pre-existing admin-analytics failures)
- Files changed: src/services/ai-agent-responders.ts
- **Learnings for future iterations:**
  - Pattern: Use .forEach() to analyze transaction text for buy/sell classification (keywords: purchase/bought/acquired vs sale/sold/disposed)
  - Pattern: Combine multiple data sources for smart money signal (institutional change + insider pattern)
  - Pattern: Calculate weighted scores for composite signals (60/40 weighting)
  - Pattern: Use netBuyCount, netSellCount, netShares from majorHoldersBreakdown if available, fallback to transaction parsing
  - Pattern: Format share counts as "K" (thousands) for readability: (netShares / 1000).toFixed(0) + 'K'
  - Pattern: Join top holder names with .slice().map().join(', ') for clean summary text
  - Gotcha: Transaction text analysis requires lowercase conversion and multiple keyword checks for accuracy
  - Gotcha: Institutional pctChange can be positive (increasing) or negative (decreasing), count both to determine trend
---

## US-091: Implement valuation response generators
Status: ✅ COMPLETE
Date: 2026-02-24
Details:
- Implemented respondValuation: Queries valuation_metrics + financial_summary, calculates P/E trailing vs forward, EV/EBITDA, FCF yield, compares to sector average P/E from up to 10 peers, returns verdict (Undervalued/Attractive/Fair Value/Premium/Expensive) based on forward P/E trend, sector comparison, and absolute multiples
- Implemented respondPeerValuation: Queries up to 5 sector peers sorted by market cap, compares target stock vs peer averages on P/E, gross margin, revenue growth, debt-to-equity, calculates premium/discount percentage, identifies specific peer companies by name (cheaper/more expensive), verdict logic considers both valuation discount AND operational metrics (margins, growth)
- Implemented respondDividendCheck: Queries dividend_info + financial_summary tables, returns dividend yield, annual rate, payout ratio, calculates FCF payout ratio for sustainability check, handles no-dividend case gracefully, formats ex-dividend date, verdict ranges from Attractive (>4% yield + sustainable) to At Risk (payout ratio >100%)
- All three functions follow established patterns: parallel Promise.all queries, n() null-safe helper throughout, opinionated summaries with **bold** key numbers and company name, dynamic verdict color logic
- Added imports: dividendInfo table, and/ne operators from drizzle-orm for peer filtering
- Sector peer comparison uses innerJoin to combine stocks + valuationMetrics/financialSummary, filters by eq(sector) and ne(stockId) to exclude target
- Dividend sustainability evaluated on both earnings payout ratio and FCF payout ratio
- Build passes with zero errors, tests pass (163/169 - same 6 pre-existing admin-analytics failures unrelated to this story)
- Files changed: src/services/ai-agent-responders.ts (+420 lines)
- **Learnings for future iterations:**
  - Pattern: Use innerJoin for cross-table queries when you need data from multiple tables in a single result set
  - Pattern: Filter peer queries with and(eq(stocks.sector, stock.sector), ne(stocks.id, stock.id))
  - Pattern: Calculate averages from peer arrays with forEach accumulation and count tracking
  - Pattern: Use .orderBy(desc(stocks.marketCap)) to get the most significant/liquid peers first
  - Pattern: Handle no-peer cases early with explicit return for Unknown verdict
  - Pattern: Calculate percentage discount/premium as ((target - avg) / avg) * 100
  - Pattern: Combine multiple criteria for verdict logic (e.g., discount + better margins = Attractive)
  - Pattern: Use .slice(0, N).map().join() for clean peer name lists in summaries
  - Pattern: Handle no-dividend case with explicit early return and neutral verdict
  - Pattern: Format dates with toLocaleDateString('en-US', { month, day, year }) for consistency
  - Pattern: Evaluate dividend sustainability from BOTH payout ratio (earnings-based) AND FCF payout ratio
  - Gotcha: Filter out extreme P/E outliers (>100) when calculating sector averages to avoid skew
  - Gotcha: Peer data queries return nested objects like peer.valuation_metrics.peRatio (not flat)
  - Gotcha: dividendYield in DB is stored as decimal (0.032) not percentage, multiply by 100 for display
  - Gotcha: Some stocks have no dividend (dividendYield null or <=0), handle gracefully with dedicated response
---
## US-092: Implement earnings quality response generators
Status: ✅ COMPLETE
Date: 2026-02-24
Details:
- Implemented respondEarnings: Queries earnings_history + financial_summary tables, calculates EQ score (0-100) based on beat rate (40%), cash conversion (30%), accruals ratio (20%), and surprise magnitude (10%), returns consecutive beats count, cash conversion ratio, accruals ratio with opinionated verdicts
- Implemented respondEarningsBeats: Queries last 6 quarters of earnings history, returns beat/miss pattern (beatsCount, missesCount, meetsCount), calculates average surprise percentage, identifies streak patterns (consecutive beats or misses), displays each quarter with surprise % and actual vs estimate EPS
- Implemented respondEarningsOutlook: Queries earnings_estimates table for periods 0q, +1q, 0y, +1y, calculates EPS growth YoY and revenue growth YoY, tracks analyst estimate revisions (up vs down in last 30d), provides forward-looking verdict based on growth expectations and revision momentum
- Verdict logic implemented: EQ score >70 = green ('Exceptional'), EQ <40 = red ('Questionable'), moderate = yellow; Beats: ≥4 of 6 = green ('Strong'), ≥4 misses = red ('Weak'), mixed = yellow; Outlook: EPS >15% + revenue >10% + positive revisions = green ('Strong Growth'), negative growth or revisions <-5 = red ('Slowing'), modest growth = green
- Summaries include opinionated language with **bold** key numbers: "exceptional earnings quality", "strong earnings execution", "strong growth trajectory", specific quarter counts and surprise percentages
- Added imports for earningsHistory and earningsEstimates tables
- Build passes, tests pass (163/169 - same 6 pre-existing admin-analytics failures unrelated to this story)
- Files changed: src/services/ai-agent-responders.ts (+333 lines)
- **Learnings for future iterations:**
  - Pattern: Use .filter() to calculate beat/miss counts from earnings history array
  - Pattern: Use .reduce() to calculate average surprise percentage across quarters
  - Pattern: Check for consecutive streaks with for loop, track streakType ('beats' | 'misses' | 'none')
  - Pattern: Calculate EQ score as weighted combination of multiple factors (beat rate, cash conversion, accruals, surprise)
  - Pattern: Query multiple estimate periods in parallel with Promise.all (0q, +1q, 0y, +1y)
  - Pattern: Calculate YoY growth as ((next - current) / current) × 100, fallback to DB epsGrowth/revenueGrowth field
  - Pattern: Track analyst revisions momentum as (revisionsUp30d - revisionsDown30d)
  - Pattern: Use .map() to transform earnings history into details array with formatted dates and values
  - Pattern: Use .toLocaleDateString with month/year format for quarter dates
  - Pattern: Type arrays explicitly when using .unshift() to avoid type inference issues
  - Gotcha: TypeScript infers array type from .map() return type, so if you need mixed status types later (like 'warning'), declare array type explicitly before map
  - Gotcha: Cash conversion ratio >100% is actually a good sign (more cash than earnings)
  - Gotcha: Accruals ratio should be low - high accruals means earnings not backed by cash
  - Gotcha: Total assets estimation is rough (debt × 3) but sufficient for accruals ratio context
  - Gotcha: Forward estimates may be missing for some stocks, handle null/undefined gracefully with n() helper
---

## US-093: Implement red flags response generators
Status: ✅ COMPLETE
Date: 2026-02-24
Details:
- Implemented respondRedFlags: Uses existing red-flag.service.ts detectRedFlags function, returns overall composite score (0-100), severity level (Low/Moderate/High/Critical), active flag count, top 3 flags sorted by weighted score with opinionated verdicts
- Implemented respondCriticalFlags: Identifies highest-severity flag by weighted score, returns full explanation with flag name, score, weight, weighted impact, description, plus lists other significant flags
- Implemented respondFlagTrend: Queries red_flag_history table for last 90 days, calculates average scores across three 30-day periods (last 30d, 30-60d ago, 60-90d ago), analyzes trend direction (increasing/decreasing/stable), determines if trend is accelerating
- Verdict logic: Score >85 = Critical Risk (red), 60-85 = High Risk (red), 30-60 = Moderate Risk (yellow), <30 = Low Risk (green)
- Summaries use direct, opinionated language: "Warning signs detected", "The biggest concern for {TICKER} is...", "Red flags are increasing and accelerating"
- Added imports: gte operator, redFlagHistory table, detectRedFlags from red-flag.service.ts
- Build passes, tests pass (163/169 - same 6 pre-existing admin-analytics failures unrelated to this story)
- Files changed: src/services/ai-agent-responders.ts (+355 lines)
- **Learnings for future iterations:**
  - Pattern: Reuse existing service functions like detectRedFlags to avoid duplication
  - Pattern: Sort arrays with .sort((a, b) => b.weighted_score - a.weighted_score) for descending order
  - Pattern: Filter active flags with .filter(f => f.score > 0) before analysis
  - Pattern: Query multiple time periods in parallel with Promise.all for trend analysis
  - Pattern: Calculate averages from array with .reduce((sum, f) => sum + n(f.score, 0), 0) / length
  - Pattern: Compare recent vs historical changes to determine if trend is accelerating
  - Pattern: Use sql template literal for date range comparisons: sql`${col} < ${date}`
  - Pattern: Handle empty history gracefully with early return and "Insufficient data" message
  - Pattern: Format flag names from snake_case to Title Case: split('_').map(capitalize).join(' ')
  - Pattern: Use Math.abs() when analyzing trend magnitude regardless of direction
  - Gotcha: TypeScript requires explicit type annotation for verdict_color: 'green' | 'yellow' | 'red'
  - Gotcha: DetectedFlagResult from red-flag.service.ts has composite_score (snake_case) not compositeScore
  - Gotcha: redFlagHistory.score is stored as doublePrecision, use n() helper for null safety
  - Gotcha: Trend analysis requires at least 2 periods of data to calculate change meaningfully
---

## US-094: Create AI agent routes with three endpoints
Status: ✅ COMPLETE
Date: 2026-02-24
Details:
- Created src/routes/ai-agent.routes.ts with Hono router and authMiddleware applied to all routes
- Implemented GET /ai-agent/questions endpoint: returns initial 6 questions by default, accepts ?parent_id= query param to return follow-up questions for that parent
- Implemented POST /ai-agent/ask endpoint: validates question_id and ticker, calls appropriate responder function based on question_id (switch statement routing to all 18 responder functions), returns response data + follow-up questions (if initial question) in snake_case format
- Implemented GET /ai-agent/usage endpoint: placeholder for US-095, returns { used: 0, limit: (tier-based), remaining, resets_at: (ISO 8601 midnight UTC next day) }
- Added export to src/routes/index.ts barrel file
- Registered routes in src/app.ts under /v1/ai-agent path
- All JSON response fields use snake_case (question_id, parent_id, follow_up_questions, verdict_color, resets_at)
- Build passes with zero errors, tests pass (163/169 - same 6 pre-existing admin-analytics failures), typecheck passes
- Files changed: src/routes/ai-agent.routes.ts (new, 207 lines), src/routes/index.ts (+1 line), src/app.ts (+2 lines)
- **Learnings for future iterations:**
  - Pattern: Use Hono router with authMiddleware applied globally via .use('*', authMiddleware)
  - Pattern: Type Variables = { requestId: string; user: AuthUser } for context type safety
  - Pattern: Use c.req.query('param_name') for query parameters, c.req.param('param_name') for path params
  - Pattern: Use c.req.json() to parse request body, validate input early with ValidationError
  - Pattern: Use switch statement for question_id routing to call appropriate responder function
  - Pattern: Calculate resets_at as Date.UTC(year, month, date + 1, 0, 0, 0) for midnight UTC next day
  - Pattern: Tier limits defined as Record<string, number> with free/pro/premium keys
  - Pattern: Return follow_up_questions array only for initial questions (parent_id === null)
  - Gotcha: GET /usage is a placeholder for US-095 - actual tracking/enforcement will be implemented there
  - Gotcha: Use getQuestionById to validate question exists, throw NotFoundError if undefined
  - Gotcha: Access user from context with c.get('user'), tier is user.tier.toLowerCase()
  - Gotcha: All response fields must be snake_case per API convention
---

## US-095: Add tier-based daily usage tracking and enforcement
Status: ✅ COMPLETE
Date: 2026-02-24
Details:
- Added TIER_LIMITS constant: FREE = 3, PRO = 15, PREMIUM = Infinity
- Implemented getUsageStats helper function: queries ai_agent_usage table for current user's daily usage (WHERE userId = ? AND date = CURRENT_DATE), calculates used/limit/remaining, returns resets_at as midnight UTC next day
- Updated POST /ai-agent/ask route: checks tier limits BEFORE generating response, queries usage count from DB, throws TierLimitError with 403 status if limit exceeded (includes used, limit, upgrade_prompt: true, resets_at in error details), inserts usage record AFTER successful response generation, includes usage object in success response
- Updated GET /ai-agent/usage route: replaced placeholder with actual getUsageStats call, returns real-time usage data from database
- TierLimitError uses 403 status (not 429) per acceptance criteria - indicates tier-based restriction vs rate limiting
- Usage tracking: inserts record with userId, questionId, ticker (uppercase), date (YYYY-MM-DD format) after successful response
- Premium tier users skip all counting and enforcement (limit = Infinity check)
- Build passes with zero errors, tests pass (163/169 - same 6 pre-existing admin-analytics failures), typecheck passes
- Files changed: src/routes/ai-agent.routes.ts (+112 lines, -32 lines)
- **Learnings for future iterations:**
  - Pattern: Define tier limits as Record<string, number> constant at module level for reusability
  - Pattern: Create helper function getUsageStats for shared logic between /ask and /usage endpoints
  - Pattern: Check limits BEFORE expensive operations (response generation), fail fast
  - Pattern: Use sql template literal for CURRENT_DATE comparison: sql`${aiAgentUsage.date} = CURRENT_DATE`
  - Pattern: Insert usage record AFTER successful response to avoid tracking failed requests
  - Pattern: Use .toISOString().split('T')[0] to get YYYY-MM-DD date format for date column
  - Pattern: Calculate resets_at as Date.UTC(year, month, date + 1, 0, 0, 0).toISOString() for midnight UTC next day
  - Pattern: Skip counting entirely for premium tier with early return to avoid unnecessary DB query
  - Pattern: TierLimitError includes upgrade_prompt: true in details for frontend to show upgrade CTA
  - Pattern: Add db null check at start of helper functions that use db
  - Gotcha: TierLimitError uses 403 status (TIER_LIMIT_EXCEEDED) not 429 (RATE_LIMITED) - different concepts
  - Gotcha: Must count .length on usageRecords array, not use COUNT(*) aggregation for simplicity
  - Gotcha: Ticker should be uppercased when inserting: ticker.toUpperCase() for consistency
  - Gotcha: Usage object included in POST /ask success response alongside response and follow_up_questions
---
